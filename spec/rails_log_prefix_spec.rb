require "spec_helper"

describe RailsLogPrefix do
  it "has a version number" do
    expect(RailsLogPrefix::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
